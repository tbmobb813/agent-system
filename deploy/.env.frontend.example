# =============================================================================
# Frontend environment — copy to frontend/.env.local on the VPS
# Never commit the real .env.local to git.
# =============================================================================

# Server-side backend URL used by the Next.js /api/backend proxy route.
# For local VM deploys this can stay on localhost.
BACKEND_URL=http://127.0.0.1:8000
